import { createBrowserRouter } from "react-router-dom";
import { PanelRoutes } from "./Panel.routes";

const routes = createBrowserRouter(PanelRoutes); // 👈 بدون براکت اضافه
export default routes;
